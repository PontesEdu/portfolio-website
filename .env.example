# URL publica do site. Unica fonte da URL usada em metadata, Open Graph,
# sitemap, robots e JSON-LD.
#
# Em producao na Vercel esta variavel e opcional: sem ela, o projeto usa o
# dominio que a propria Vercel injeta. Defina-a quando houver dominio proprio.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
